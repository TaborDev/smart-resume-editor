{
  "name": "@smart-resume/types",
  "version": "1.0.0",
  "description": "Shared TypeScript types for Smart Resume Editor",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "clean": "rm -rf dist",
    "type-check": "tsc --noEmit",
    "test": "jest",
    "lint": "echo \"No linting configured for types package\""
  },
  "devDependencies": {
    "typescript": "^5.3.0",
    "@types/jest": "^29.5.8",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.1"
  },
  "files": [
    "dist/**/*"
  ]
}